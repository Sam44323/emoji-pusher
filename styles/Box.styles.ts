import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  titleText: {
    fontSize: 18,
    fontWeight: "700",
  },
  pressed: {
    opacity: 0.75,
  },
});

export default styles;
